# Secrets — never commit
.env
.env.*
local.settings.json

# OS / editor
.DS_Store
.vscode/
.idea/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Generated artefacts (regenerate with fixtures/generate_fixtures.py and templates/build_excel.py)
fixtures/samples/
*.xlsx
*.png
out/

# Merchant memory is a reviewed product configuration file. The builder emits
# only explicitly approved exceptional keys and strips applicant-specific data,
# so the safe result is committed and deployed from the same Git revision.

# Material received from Vikas, not ours to publish in a public repo.
from vikas/
